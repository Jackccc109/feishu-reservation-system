// app.js
App({
  globalData: {
    // 后端服务地址
    // 本地联调（真机）：用电脑的局域网 IP，手机和电脑连同一 Wi-Fi 才能访问
    // 开发者工具模拟器：可用 http://localhost:3000
    // 生产上线：必须是 HTTPS 且加入小程序「request 合法域名」，HTTP 无法上线
    BASE_URL: 'http://10.220.185.3:3000',
    // 门店编码（多门店路由：顾客端请求自动带上，服务端按门店隔离数据）
    // 对应管理端「员工管理 → 配置门店」里的门店编码
    storeCode: 'MI78295'
  },
  onLaunch() {}
});
