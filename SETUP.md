# 飞书版预约签到系统 - 部署指南

## 架构

```
┌─────────────┐     HTTP API      ┌──────────────┐     Base API     ┌──────────────┐
│  H5 前端页面  │ ───────────────▶ │  Node.js 服务  │ ──────────────▶ │  飞书多维表格  │
│ (任意设备)    │ ◀─────────────── │  (Render等)   │ ◀────────────── │  (数据库)     │
└─────────────┘                   └──────────────┘                  └──────────────┘
```

- **前端**：和原来一模一样的 UI（日历选择、二维码、管理后台）
- **后端**：Node.js + Express，通过飞书 Base API 读写数据
- **数据库**：飞书多维表格，所有设备共享同一份数据
- **部署**：部署到支持 Node.js 的云平台（Render.com / Railway.app）

---

## 第一步：创建飞书多维表格

### 1. 打开飞书 → 新建多维表格

### 2. 创建字段（按以下顺序创建）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| 预约码 | 文本 | 6位字母数字，唯一标识 |
| 签到验证码 | 文本 | 6位字母数字 |
| 姓名 | 文本 | 顾客姓名 |
| 手机号 | 文本 | 11位手机号 |
| 到店人数 | 数字 | 1~6 |
| 预约日期 | 文本 | 格式 YYYY-MM-DD |
| 预约时段 | 文本 | 如 "10:00-10:20" |
| 体验场景 | 文本 | 人车家场景 / 智家场景 |
| 排队号 | 数字 | 同时段内序号 |
| 状态 | 文本 | 待签到 / 已签到 / 已取消 |
| 创建时间 | 文本 | ISO 时间格式 |
| 签到时间 | 文本 | ISO 时间格式（可空） |

### 3. 创建视图（方便门店管理）

#### 视图1：今日待签到
- 筛选条件：状态 = "待签到"
- 排序：预约时段 升序

#### 视图2：全部记录
- 无筛选，按创建时间降序

---

## 第二步：创建飞书应用（获取 API 凭据）

### 1. 打开飞书开放平台
https://open.feishu.cn/

### 2. 创建自建应用
- 应用名称：预约签到系统
- 头像：随意

### 3. 获取 App ID 和 App Secret
进入应用 → 凭证与基础信息 → 复制 App ID 和 App Secret

### 4. 开通权限
进入应用 → 权限管理 → 搜索 "bitable" → 添加以下权限：
- `bitable:app` （多维表格）

### 5. 发布应用
- 创建应用版本并发布（审核秒过）
- 在多维表格中将应用添加为协作者（编辑权限）

### 6. 获取 Base Token 和 Table ID

**方法一：从 URL 获取**
飞书 Base URL：`https://abc.feishu.cn/base/BITABLEappXXXXXXXXX?table=tblYYYYYYY`
- `app_token` = `BITABLEappXXXXXXXXX`
- `table_id` = `tblYYYYYYY`

**方法二：API 调试台**
飞书开放平台 → API 调试台 → 多维表格 → 列出数据表

---

## 第三步：部署服务

### 方案A：Render.com（推荐，免费）

1. 注册 https://render.com
2. New → Web Service
3. 连接 GitHub 仓库（或上传代码）
4. 设置：
   - Build Command: `npm install`
   - Start Command: `node server.js`
5. 添加环境变量（Settings → Environment）：
   ```
   FEISHU_APP_ID=cli_xxxxx
   FEISHU_APP_SECRET=xxxxx
   FEISHU_APP_TOKEN=BITABLEappxxxxx
   FEISHU_TABLE_ID=tblxxxxx
   BASE_URL=https://your-app.onrender.com
   ```
6. Deploy！获得链接如 `https://reservation-xxxx.onrender.com`

### 方案B：Railway.app

1. 注册 https://railway.app
2. New Project → Deploy from GitHub
3. 添加环境变量同上
4. 自动部署，获得链接

### 方案C：本地运行 + 内网穿透

```bash
# 1. 复制环境变量
cp .env.example .env
# 编辑 .env 填入飞书配置

# 2. 安装依赖
npm install

# 3. 启动
npm start

# 4. 内网穿透（新终端）
npx localtunnel --port 3000
# 获得公网链接，填入 BASE_URL
```

---

## 第四步：嵌入飞书

部署成功后，获得公网链接 `https://your-app.xxx.com`：

### 顾客端
直接分享 `https://your-app.xxx.com` 给顾客（微信扫码即可打开）

### 管理端
`https://your-app.xxx.com/admin` 给门店员工使用

### 嵌入飞书工作台（可选）
飞书开放平台 → 应用 → 应用功能 → 网页应用 → 添加：
- 桌面端主页：`https://your-app.xxx.com/admin`
- 移动端主页：`https://your-app.xxx.com`

---

## 使用流程

```
顾客：
  打开链接 → 选日期时段 → 填信息 → 提交
  → 获得二维码+验证码 → 截屏 → 到店出示

店员（管理端）：
  打开管理链接 → 三种签到方式任选其一
  → 统计看板 / 排队看板 / 预约列表 一目了然
```

---

## 时段配置

共 36 个时段，20 分钟/段，10:00-22:00：
```
10:00-10:20  10:20-10:40  10:40-11:00  11:00-11:20  11:20-11:40  11:40-12:00
12:00-12:20  12:20-12:40  12:40-13:00  13:00-13:20  13:20-13:40  13:40-14:00
14:00-14:20  14:20-14:40  14:40-15:00  15:00-15:20  15:20-15:40  15:40-16:00
16:00-16:20  16:20-16:40  16:40-17:00  17:00-17:20  17:20-17:40  17:40-18:00
18:00-18:20  18:20-18:40  18:40-19:00  19:00-19:20  19:20-19:40  19:40-20:00
20:00-20:20  20:20-20:40  20:40-21:00  21:00-21:20  21:20-21:40  21:40-22:00
```

每个时段最多 1 组预约（可在 .env 中修改）。
